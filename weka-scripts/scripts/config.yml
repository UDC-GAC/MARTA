# File              : config.yml
# Author            : Marcos Horro <marcos.horro@udc.gal>
# Date              : Mér 06 Nov 2019 16:36:12 MST
# Last Modified Date: Mér 06 Nov 2019 22:26:40 MST
# Last Modified By  : Marcos Horro <marcos.horro@udc.gal>
#
# example file, this file should be used for executing with wrapper.py
# POSSIBLE VALUES
#- files:
#    input: # existing valid filename
#    output: # valid filename
#    overwrite: # True/False
#    clean: # True/False
#
#- prepare_data:
#    cols: # columns to use for DT
#    rows: # filtering rows in CSV
#    pred: # dimension to predict
#    norm: # norm values
#    norm_alg: # z-score/minmax; useless if norm: False
#    ncats: # use 0 for discrete values, value greater than 1 in other case
#    dt_alg: # Only supported REPTree right now
#    dt_params: # e.g. M 5 N 1 (minimum 5 instances, 1 fold)
#- recommender:
#    value: # value of interest
#    interest_values: # values of interest

- files:
    input: exec_asm_full_filtered.csv
    output: output.arff
    overwrite: True # overwrite if existing files?
    clean: True # clean temp files?
- prepare_data:
    cols: I J It Jt Is Js Jters Iters vmov_loads_mem_pd
    rows: I=0 J=0
    pred: FLOPSs # dimension to predict
    norm: False # norm values
    norm_alg: None # z-score/minmax; useless if norm: False
    ncats: 0 # use 0 for discrete values
    dt_alg: REPTree # FIXME Only supported right now
    dt_params: M=5 N=5
- recommender:
    interest_value: value
    dimensions: values
