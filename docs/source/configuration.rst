Configuration
=============

MARTA requires configuration files for both modules: the profiler and the
analyzer. These files are in YAML format, and MARTA also provides tools for
checking whether a config file is valid or not.

.. toctree::
   :maxdepth: 4

   config/profiler
   config/analyzer